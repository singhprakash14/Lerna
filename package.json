{
  "name": "analystt",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --scope=web-app",
    "build": "lerna run build --scope=web-app"
  },
  "devDependencies": {
    "css-loader": "^6.10.0",
    "lerna": "^7.4.2",
    "style-loader": "^3.3.4"
  },
  "dependencies": {
    "bootstrap": "^5.3.2"
  }
}
